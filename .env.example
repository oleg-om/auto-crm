PORT=8090
NODE_ENV=dev
APP="Autodom CRM"
ENABLE_SOCKETS=true
MONGO_URL="mongodb+srv://..."
# Shared secret the external site sends as the `x-api-key` header when
# posting to public endpoints (e.g. POST /api/v1/tyre) without a CRM login.
EXTERNAL_API_KEY="..."
