# Optional vhost for your real domain (uncomment and set server_name).
# Requests still work via default.conf (server_name _) until you customize this.
#
# upstream backend_domain {
#     server app:8090;
# }
#
# server {
#     listen 80;
#     server_name your-domain.com www.your-domain.com;
#     root /app/public;
#
#     location / {
#         try_files $uri @backend_domain;
#     }
#
#     location @backend_domain {
#         proxy_pass http://backend_domain;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header Host $host;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
