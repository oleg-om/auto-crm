// Default for settings.journalWorkTimeToleranceMin (server/model/settings.js) - minutes a work day
// may start late / end early before the boss journal flags it. Used until the value is saved on
// /settings.
// eslint-disable-next-line import/prefer-default-export
export const DEFAULT_JOURNAL_WORK_TIME_TOLERANCE_MIN = 10
