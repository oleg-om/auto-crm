// Categories for the "Грузовой"/"Сельхоз" directions - unlike "Легковые" (picked from the
// dynamic categorys store), these are hardcoded, matching the original form. Not reachable from
// the UI today (window-type-list.js only exposes legk) but kept for legacy records of this type.
module.exports = [
  { name: 'Основное', value: 'common' },
  { name: 'Другое', value: 'other' }
]
